PREVIOUS PROGRAM (already delivered in this conversation - this is an EDIT):
{{ previous_ls }}

EDIT RULES (strict):
- Apply the SMALLEST change that satisfies the TASK section below.
- Every other line stays byte-identical: same moves, same signals, same
  comments, same speeds, same program name (unless the task renames it).
- Do not add, remove or reorder anything the task did not ask about.
