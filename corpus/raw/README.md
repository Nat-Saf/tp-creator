Place the 3 FANUC controller PDFs here (proprietary - internal GitLab only;
*.pdf is gitignored by default: either use Git LFS or keep this folder local
and note the network share path here instead).
